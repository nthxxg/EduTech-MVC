@{
    Layout = "_Layout";
    ViewData["Title"] = "Bad Request - Yêu cầu không hợp lệ";
    string message = ViewBag.Message ?? "Yêu cầu không hợp lệ.";
}

<div class="container">
    <div class="row justify-content-center align-items-center min-vh-100">
        <div class="col-md-6 text-center">
            <div class="card shadow-sm border-2">
                <div class="card-body p-5">
                    <i class="bi bi-exclamation-triangle text-warning" style="font-size: 5rem;"></i>
                    <h1 class="mt-4 mb-3">400</h1>
                    <h2 class="mb-3">Yêu cầu không hợp lệ</h2>
                    <p class="mb-4">@message</p>
                    <a href="@Url.Action("Index", "Home")" class="btn btn-primary">
                        <i class="bi bi-house-door me-2"></i>Quay lại trang chủ
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>